# parse given XML and generate a DBT file that contains detail of input tables

- Create a folder

* Create a python file (file_name.py) and write code
* add the given xml file in that folder
* Then run the python file
